import type { NextPage } from 'next';

import HomeTemplate from '@components/templates/home-template';

const Home: NextPage = () => (
  <HomeTemplate>
    <div>메인 홈 입니다.</div>
  </HomeTemplate>
);

export default Home;
